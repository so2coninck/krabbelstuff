
<%= @task.name %>

<%= @task.description %>

<%= link_to "all the tasks", livre_d_or_path %>

<%= link_to "edit this task", edit_task_path(@task) %>

<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>



  <%= link_to 'Back', livredor_path %>


</div>

  </div>
</div>
